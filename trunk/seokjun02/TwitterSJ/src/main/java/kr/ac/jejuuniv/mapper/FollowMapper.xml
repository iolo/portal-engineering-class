<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.ac.jejuuniv.mapper.FollowMapper">
	<select id="checkFollow" parameterType="String" resultType="String">
		Select followid From follow Where userid = #{userid}
	</select>
	<select id="selectFollowingUser" parameterType="String" resultType="kr.ac.jejuuniv.model.User">
		SELECT twituser.username AS userName,
			twituser.introduce AS introduce,
			twituser.photo AS photo
		FROM twituser, follow
		WHERE twituser.userid =	follow.followid
		AND follow.userid = #{userid} ORDER BY twituser.username ASC
	</select>
	<select id="selectFollowerUser" parameterType="String" resultType="kr.ac.jejuuniv.model.User">
		SELECT twituser.username AS userName,
			twituser.introduce AS introduce,
			twituser.photo AS photo
		FROM twituser, follow
		WHERE twituser.userid =	follow.userid
		AND follow.followid = #{userid} ORDER BY twituser.username ASC
	</select>
</mapper>