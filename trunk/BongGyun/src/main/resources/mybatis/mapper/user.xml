<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.bong.replyboard.repository.UserDao">
	<insert id="insertUser" parameterType="kr.bong.replyboard.model.User">
		INSERT INTO user (id, name, password, description, profPath)
		VALUES (#{id}, #{name}, #{password}, #{description}, #{profPath})
	</insert>
	
	<update id="updateUser" parameterType="kr.bong.replyboard.model.User">
		UPDATE user  
		   SET name = #{name}
		     , password = #{password}
		     , description = #{description}
		     , profPath = #{profPath}
		 WHERE id = #{id}
	</update>
	
	<delete id="deleteUser" parameterType="String">
		DELETE  
		  FROM user
		 WHERE id = #{id}
	</delete>
	
	<select id="selectUser" parameterType="String" resultType="kr.bong.replyboard.model.User">
		SELECT id, name, password, description, profPath 
		  FROM user 
		 WHERE id = #{id}
	</select>
	
</mapper>