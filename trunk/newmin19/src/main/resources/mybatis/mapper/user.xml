<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.mg.comment.repository.UserDao">
	<select id="getUser" parameterType="String" resultType="kr.mg.comment.model.User">
		SELECT * FROM user WHERE id = #{id}
	</select>
	
	<insert id="insertUser" parameterType="kr.mg.comment.model.User">
		INSERT INTO user (id, name, password, description, image)
		VALUES (#{id}, #{name}, #{password}, #{description}, #{image})
	</insert>
	
	<update id="modify" parameterType="kr.mg.comment.model.User">
		UPDATE user  
		   SET name = #{name}
		     , password = #{password}
		     , description = #{description}
		     , image = #{image}
		 WHERE id = #{id}
	</update>
	
	<select id="getList" parameterType="String" resultType="kr.mg.comment.model.User">
		SELECT * FROM user
	</select>
	
</mapper>