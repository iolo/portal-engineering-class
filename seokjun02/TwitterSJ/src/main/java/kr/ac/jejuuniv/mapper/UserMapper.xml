<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.ac.jejuuniv.mapper.UserMapper">

	<select id="checkUser" parameterType="map" resultType="int">
		Select
		count(userid) From twituser Where userid=#{userId} and password = #{password}
	</select>
	<select id="checkId" parameterType="String" resultType="int">
		Select
		count(userid) From twituser Where userid=#{userId}
	</select>
	<select id="selectUser" parameterType="String" resultType="kr.ac.jejuuniv.model.User">
		Select userid, password, username, introduce, photo From twituser Where userid = #{userId}
	</select>
	<insert id="insertUser" parameterType="kr.ac.jejuuniv.model.User">
		insert into twituser (userid,
		password, username, introduce, photo)
		values (#{userId}, #{password}, #{userName},
		#{introduce}, #{photo})
	</insert>
	<update id="updateUser" parameterType="kr.ac.jejuuniv.model.User">
		update twituser set password = #{password},
			username = #{userName},
			introduce = #{introduce},
			photo =	#{photo}
		where userid = #{userId}
	</update>
	<select id="selectAllUser" resultType="kr.ac.jejuuniv.model.User">
		Select userid, username, introduce, photo From twituser Order By username asc
	</select>
	<select id="checkFollow" parameterType="String" resultType="String">
		Select followid From follow Where userid = #{userid}
	</select>
	<select id="selectFollowingUser" parameterType="String" resultType="kr.ac.jejuuniv.model.User">
		Select followid From follow Where userid = #{userid}
	</select>
</mapper>